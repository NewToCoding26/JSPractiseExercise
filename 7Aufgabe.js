       /* Write a JavaScript program to extract the first half of a
        string of even length. */





























        const extractString = (str) => str.slice(0, str.length / 2);

        console.log(extractString('nein'));
        console.log(extractString('neinundnie'));